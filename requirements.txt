# Python 3.9
#   conda create -n abb_rockbolt_env python=3.9 -y
#   conda activate abb_rockbolt_env
#   pip install -r requirements.txt
#
# Detectron2 is not on PyPI and is built against the installed torch, so it is
# installed separately, after this file:
#   pip install git+https://github.com/facebookresearch/detectron2.git

# --- Core deep learning ---
torch>=1.13
torchvision>=0.14

# --- Data handling ---
numpy>=1.21,<2.0
pandas>=1.3
opencv-python>=4.5
matplotlib>=3.5

# --- Point cloud processing ---
open3d>=0.16

# --- Image augmentation (Mask R-CNN training) ---
albumentations>=1.3

# --- Scientific utilities ---
scipy>=1.7
scikit-learn>=1.0

# --- COCO annotation support for Detectron2 ---
pycocotools>=2.0.4

# --- Misc ---
pyyaml>=5.4
Pillow>=9.0
tqdm>=4.62
